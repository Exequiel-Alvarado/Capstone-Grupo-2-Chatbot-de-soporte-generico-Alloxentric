# docs/

Documentación técnica versionada del proyecto.

- `adr/NNN-titulo.md` — un ADR corto por cada decisión de arquitectura
  (Postgres+pgvector en vez de Qdrant, arq en vez de Celery, JWT propio, etc.)

Los borradores y diagramas en crudo van en `recursos/avances/arquitectura/`.
